import { askClaude, extractJson, jsonHandler, bioBlock } from "./_lib/claude.js";

// Раньше сюда подмешивался скилл thumbnail-generation с большим сводом запретов
// (не рисовать интерфейс, максимум 3 элемента, палитры по таблице и т.д.) —
// на практике эти ограничения давали пустые скучные обложки. Теперь наоборот:
// генератору даётся свобода нарисовать насыщенную сцену, а точечные правки
// пользователь вносит вручную через images.edit (поле «Что исправить» на карточке).
const SYSTEM = `Ты — арт-директор YouTube-канала AI Music Lab (русскоязычный канал про создание музыки в Suno). Твоя задача — написать промпт для генерации обложки через OpenAI gpt-image-2. Картинка рождается целиком за один запрос: сцена, текст, эффекты — всё в одном промпте.

ГЛАВНАЯ ЦЕЛЬ — БОГАТАЯ, ЗАПОЛНЕННАЯ КАРТИНКА. Ориентир — обложки топовых обучающих каналов: плотная композиция, насыщенные цвета, свечение, энергия, движение. Пустой тёмный фон с одинокой фигурой и парой прямоугольников — это провал. Смело наполняй кадр: макет интерфейса с подписями, летящие ноты, инструменты (гитара, микрофон, наушники, колонки), стрелки от причины к результату, искры/взрыв света в точке превращения, эмоциональный человек, реагирующий на происходящее. Лучше слегка перегрузить кадр, чем оставить пустоту — у пользователя есть окно ручных правок, убрать лишнее проще, чем добавить недостающее.

ИСТОЧНИК СОДЕРЖАНИЯ: строка "Тема" — черновая заметка, а настоящий источник — "Контекст" (пересказ + сценарий). Строй сцену вокруг того, что реально происходит в видео: конкретное действие, конкретный интерфейс, конкретный результат. Обложка должна отвечать на вопрос зрителя «что я увижу внутри?» одним взглядом.

ИНТЕРФЕЙС РИСОВАТЬ МОЖНО И НУЖНО, если видео про работу в приложении: стилизованный макет окна с полями, подписями и кнопкой — рабочий приём. gpt-image-2 нормально рисует короткие подписи, в том числе по-русски: заголовки полей, слово на кнопке, плейсхолдер в одну строку. Не проси только многострочные абзацы мелкого текста — они превратятся в кашу. Поля и кнопки делай светящимися, цветными (у каждого поля свой неоновый цвет обводки), не плоскими серыми.

ТЕКСТ НА ОБЛОЖКЕ: один крупный заголовок по-русски, 3-6 слов, жирный шрифт без засечек, белый и/или жёлтый с толстой чёрной обводкой, в верхней части кадра. Можно добавить стрелку → внутри заголовка (например «2 ПОЛЯ → ГОТОВАЯ ПЕСНЯ»). Плюс допустимы короткие подписи-лейблы внутри сцены (слово на кнопке, названия полей).

ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ (это механика нашего пайплайна, их соблюдай всегда):
1. Про верхний край: gpt-image-2 иногда рисует буквы заголовка впритык к верху холста и сам их обрезает. Опиши пустую полосу сверху как ЭЛЕМЕНТ СЦЕНЫ: сначала "the top ~10% of the frame is empty background", потом "below that, the bold title begins". Именно в таком порядке.
2. Зона обрезки по текущему формату указана в блоке "Формат" — важные детали туда не клади.
3. Не размещай ключевые детали в нижнем правом углу 16:9 — там YouTube рисует плашку длительности.
4. Стиль: photorealistic where people appear, natural skin, professional color grading, high contrast, sharp focus; no plastic skin, no distorted hands.

ФОРМАТ ОТВЕТА: верни строго JSON без пояснений:
{"prompt": "готовый промпт на английском для OpenAI Images API — одна связная сцена: композиция, персонаж и его эмоция, макет интерфейса/объекты, эффекты, свет, все надписи (заголовок и подписи — по-русски в кавычках), с учётом соотношения сторон и зон обрезки"}`;

const FORMAT_NOTES = {
  "16:9":
    "YouTube Long thumbnail. Финальный кадр 1536×864 (16:9). Генерируется в 1536×1024, затем кодом обрезается ПОЛНОСТЬЮ снизу (~16% высоты); сверху не срезается ничего. Заголовок в верхней части в безопасности, а в нижних ~16% кадра важного не размещай.",
  "9:16":
    "YouTube Shorts cover. Финальный кадр 864×1536 (9:16). Генерируется в 1024×1536, затем кодом обрезается ПОЛНОСТЬЮ справа (~16% ширины); слева не срезается ничего. Важные детали в правых ~16% кадра будут срезаны гарантированно.",
  "1:1": "Community post image, квадрат 1:1 — генерируется точно в размер, обрезка не требуется, можно использовать весь кадр.",
};

const SHORTS_STYLE_NOTE =
  "ФОРМАТ 9:16: хорошо работает вертикальный стек из 2-4 коротких текстовых баннеров на контрастных цветных плашках (затравка → крупный заголовок → уточнение) + человек с яркой эмоцией + насыщенная сцена по теме. ВАЖНО (механика обрезки): баннеры не прижимай к левому краю — требуй в промпте отступ, например \"banners start with a clear margin from the left edge\", иначе первые буквы могут пострадать при генерации у самого края.";

const VARIANT_NOTE = {
  A: "Это ВАРИАНТ A для A/Б-теста обложек на YouTube. Сделай самую сильную, насыщенную версию сцены.",
  B: "Это ВАРИАНТ Б для A/Б-теста обложек на YouTube. Он должен СОДЕРЖАТЕЛЬНО отличаться от варианта A: другой текст заголовка, другая композиция/ракурс и другая цветовая гамма — но такая же плотная и конкретная по теме, не уходи в пустой атмосферный фон.",
};

export default jsonHandler(async (body) => {
  const { topic, aspect = "16:9", context, variant, channelBio } = body;
  if (!topic) throw new Error("Не указана тема для обложки");
  const trimmedContext = context ? context.slice(0, 6000) : "";
  const variantNote = VARIANT_NOTE[variant] ? `\n\n${VARIANT_NOTE[variant]}` : "";
  const shortsNote = aspect === "9:16" ? `\n\n${SHORTS_STYLE_NOTE}` : "";
  const text = await askClaude({
    system: SYSTEM,
    user: `${bioBlock(channelBio)}Тема: ${topic}\n\nФормат: ${FORMAT_NOTES[aspect] || aspect}${trimmedContext ? `\n\nКонтекст (сценарий/описание/пост — строй сцену из его конкретики):\n${trimmedContext}` : ""}${shortsNote}${variantNote}\n\nСгенерируй промпт обложки.`,
    maxTokens: 2000,
  });
  return extractJson(text);
});
